name: textradeos-local

services:
  backend:
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      APP_VERSION: 0.0.0

  frontend:
    build:
      context: ../../TexTradeOS-PRO
      dockerfile: Dockerfile
