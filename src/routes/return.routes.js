import {Router} from "express";import {requireAuth} from "../middleware/auth.middleware.js";import {ReturnController} from "../controllers/return.controller.js";const router=Router();router.use(requireAuth);router.get("/sales/returnable/:partyId",ReturnController.returnable);router.get("/:type",ReturnController.list);router.get("/:type/:id",ReturnController.get);router.post("/:type",ReturnController.create);router.delete("/:type/:id",ReturnController.delete);export default router;
